<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.zcx.crm.workbench.dao.CustomerRemarkDao">

    <select id="getRemarkListByCid" resultType="CustomerRemark">
        select * from tbl_customer_remark where customerId = #{customerId} order by createTime desc, editTime desc
    </select>

    <insert id="save">
        insert into tbl_customer_remark(
            id,
            noteContent,
            createTime,
            createBy,
            editFlag,
            customerId
        )
        values (
            #{id},
            #{noteContent},
            #{createTime},
            #{createBy},
            #{editFlag},
            #{customerId}
        )
    </insert>

</mapper>

































